{
  "name": "src",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "slidev build --base /sandbox/ --out ../../dist/sandbox",
    "dev": "slidev --open",
    "export": "slidev export --dark --output ../sandbox.pdf"
  }
}